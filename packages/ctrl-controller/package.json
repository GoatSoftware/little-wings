{
  "name": "@little-wings/ctrl-controller",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "predev": "npm run clean",
    "dev": "parcel src/index.html --https",
    "clean": "rimraf dist",
    "prebuild": "npm run clean",
    "build": "parcel build src/index.html src/controller.html src/resources/**/*",
    "start": "node index.js"
  },
  "author": "GoatSoft",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "socket.io-client": "^2.3.0"
  },
  "devDependencies": {
    "@types/socket.io-client": "^1.4.32",
    "parcel-bundler": "^1.12.4",
    "rimraf": "^3.0.0"
  }
}
